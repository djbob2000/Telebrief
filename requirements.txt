# Core dependencies
telethon>=1.34.0
python-telegram-bot>=20.0
openai>=1.0.0
APScheduler>=3.10.0
python-dotenv>=1.0.0
PyYAML>=6.0
aiohttp>=3.9.0

# MCP server (optional feature, always installed for a single-image build)
mcp>=2.0.0
uvicorn>=0.30.0

# Utilities
python-dateutil>=2.8.0

# Storage backends (aiosqlite for SQLite, asyncpg for Postgres)
aiosqlite>=0.22.1
asyncpg>=0.31.0

# Domain persistence & job orchestration (PostgreSQL migration phase)
psycopg[binary]>=3.3.4
psycopg-pool>=3.3.1
pgvector>=0.5.0
procrastinate>=3.9.0
playwright>=1.62.0
