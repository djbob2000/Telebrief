"""Facebook provider module.

NOTE (Architecture Preservation):
The Facebook provider, its jobs (src/jobs/facebook.py), and its repositories
(src/repositories/facebook.py) are intentionally preserved in the codebase.
The feature is currently disabled by configuration (facebook.enabled: false),
with activation, live testing, and verification scheduled for upcoming releases.
Do NOT delete or purge this module during dead-code cleanups.
"""
